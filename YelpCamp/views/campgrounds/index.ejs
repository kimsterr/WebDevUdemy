<% layout('layouts/boilerplate') %>
    <h1>All the campgrounds</h1>
    <div>
        <a href="/campgrounds/new">Add a new campground</a>
    </div>

    <ul>
        <% for (let cmpgd of campgrounds) {%>
            <li>
                <a href="/campgrounds/<%= cmpgd._id %>">
                    <%= cmpgd.title %>
                </a>
            </li>
            <% } %>
    </ul>